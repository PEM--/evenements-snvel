const { Views } = MainApp;

const VerifiedEmail = () => {
  return (
    <section className='maximized MainContent animated fadeIn'>
      <h1>Email vérifié</h1>
    </section>
  );
};

MainApp.Views.VerifiedEmail = VerifiedEmail;
